# 工作流路由

根据任务类型选择入口流程:

- **新功能 / 大改动** (涉及多文件、新模块、架构变更): brainstorming → writing-plans → plan-review → 开分支 → implement → code-review → documentation-sync → 用户确认后 commit
- **小修改 / bug 修复** (已知问题、单文件改动): grilling → 开分支 → implement → code-review → documentation-sync → 用户确认后 commit

不确定属于哪一类时, 先询问用户。
